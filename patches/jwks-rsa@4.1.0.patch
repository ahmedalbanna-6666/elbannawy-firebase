diff --git a/src/utils.js b/src/utils.js
index ef3ffb56cda03d2f02d655c55ad14ad983223273..958629238220d00eebc236c38fd8f59a2f910214 100644
--- a/src/utils.js
+++ b/src/utils.js
@@ -1,5 +1,9 @@
-const jose = require('jose');
 const JwksError = require('./errors/JwksError');
+let josePromise;
+function getJose() {
+  if (!josePromise) josePromise = import('jose');
+  return josePromise;
+}
 
 function resolveAlg(jwk) {
   if (jwk.alg) {
@@ -41,6 +45,7 @@ async function retrieveSigningKeys(jwks) {
 
   for (const jwk of jwks) {
     try {
+      const jose = await getJose();
       const key = await jose.importJWK({ ...jwk, ext: true }, resolveAlg(jwk));
       if (key.type !== 'public') {
         continue;
